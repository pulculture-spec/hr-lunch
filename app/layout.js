export const metadata = { title: '인사혁신팀 점심 일정' }

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body style={{ margin: 0, background: '#fafaf8' }}>{children}</body>
    </html>
  )
}
